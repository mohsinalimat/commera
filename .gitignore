# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
# bench symlinks public/node_modules to the app root; the trailing-slash rule above
# only matches real directories, so the symlink needs its own line.
node_modules
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*

commera/public/css/tailwind.css

# Local agent config
.claude/

# Commera build output — generated by `npm run build` in dashboard/, never committed.
commera/public/commera/
commera/www/commera.html

# The same build output under its pre-rename path. Left behind by an older build and
# dead since; ignored so a wide `git add` cannot sweep it back in.
commera/public/dashboard/
commera/www/dashboard.html

# The Pixio vendor theme the Summer theme derives from, kept locally as the provenance reference.
# 14MB of demo pages and duplicated CSS that nothing loads at build or run time.
reference_themes/

# Commera e2e artifacts — failure screenshots and the cached login session.
dashboard/tests/e2e/.artifacts/
